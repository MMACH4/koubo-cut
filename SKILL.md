---
name: koubo-cut
description: 口播视频（talking-head 一镜到底）精剪 + 剪映工程包装。当用户提供口播/发布会/探店/讲解类原片并要求剪辑、加字幕包装、生成剪映工程时使用。流程：上传素材先压缩到统一1080P（横1920x1080/竖1080x1920）→ 由用户确认横竖屏与包装强度（低2/中5/高10 个每分钟，均需用户选择后才执行）→ 生成字幕/花字/标题/音效 → 精剪（保留最完整句）→ 剪映中生成每段都有独立出入点的工程，命名「核心信息不超过5字-cut」。
---

# Koubo Cut 口播剪辑

面向中文口播视频的「精剪 + 剪映包装」流水线。单机位口播不做硬切，节奏靠声音和字幕；所有包装（标题/花字/重点字幕）都必须配音效。

## 流程

1. **预处理**：`scripts/preprocess.py <原片>` 压缩到统一 1080P（横 1920×1080、竖 1080×1920），输出压缩片路径与方向（H/V）。
2. **人工确认方向与强度（必须先完成，才执行后续）**：向用户展示原片信息，并请用户确认：
   - ① **横版还是竖版**：由用户人工确认，以用户选择为准（自动探测仅作参考）；
   - ② **包装强度**：低 2/分钟、中 5/分钟（默认）、高 10/分钟。
   两项都确认后，才进入转写与剪辑。
3. **转写**：`scripts/transcribe.py <压缩片> --out-dir <工作目录>`，产出 `transcript.json`（含字级时间戳）与 `transcript.srt`。
4. **精剪（Agent 判断）**：阅读转写，按编辑规则删半句/重说/口误，保留最完整句；生成 `cut_plan.json`（保留区间 + 字幕文本）。
5. **包装计划**：按用户确认的强度生成 `packaging_plan.json`（标题/花字/重点字幕及其时刻与文本）。规则见 `references/editing-rules.md`。
6. **生成工程**：`scripts/build_draft.py --plan plan.json --work-dir <工作目录>` 生成剪映工程：
   - 主视频 = 压缩片按剪辑点切段，**每段独立出入点（source_timerange）**，可逐段微调；
   - 字幕（超长自动拆两段）、标题、花字、重点高亮、音效；无角标。
7. **命名**：抓取视频核心信息，≤5 字 + `-cut`，如 `2万打卡-cut`。

## 关键规则（先读 references/editing-rules.md）

- 编辑：语速快、气口剪紧；说错立马重说时取最完整、最连贯、信息量足的那句。
- 包装强度：低 2/min、中 5/min、高 10/min（全部带音效，含花字与标题）。
- 竖版与横版的字幕/标题/花字位置与字号不同，见规则文件。
- 依赖：ffmpeg（可用环境变量 `KOUBO_FFMPEG`/`KOUBO_FFPROBE` 覆盖默认路径）、faster-whisper small 模型、`jianying-editor` skill（本机已安装）、剪映本地字体「极宋」（缺失时回退默认字体）。

## 产出

- 剪映草稿工程（交付物，不导出成片，用户自行精修）。
- 工程名：`<核心信息≤5字>-cut`。
